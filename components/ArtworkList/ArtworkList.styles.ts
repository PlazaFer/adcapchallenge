import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  grid: {
    gap: 15,
    width: "100%",
  },
  scrollContainer: {
    paddingBottom: 20,
  },
  activityLoaderScroll: {
    marginTop: 24,
  },
});

export default styles;
